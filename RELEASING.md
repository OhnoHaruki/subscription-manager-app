# リリースガイド

## 1. プロダクション環境への切り替え

現在は開発用の Supabase プロジェクトを使用しています。リリース時には以下の手順でプロダクション環境へ切り替えてください。

### Supabase の設定
1. プロダクション用の Supabase プロジェクトを新規作成します。
2. `supabase/migrations` の各SQLファイルを順に適用して、テーブル構造とRLSポリシーを構築します。
3. `supabase/functions/delete-user` をデプロイします。
   ```bash
   supabase functions deploy delete-user --project-ref <prod-project-id>
   ```

### アプリの設定
1. `.env` ファイルにプロダクション用の URL と Anon Key を設定します。
2. `lib/main.dart` で `.env` が読み込まれていることを確認してください。

## 2. バージョン管理

`pubspec.yaml` の `version` フィールドで管理します。

- 形式: `x.y.z+n`
  - `x.y.z`: セマンティックバージョニング（メジャー.マイナー.パッチ）
  - `n`: ビルド番号（リリースごとに必ずインクリメントする）
- 例: 初回リリース `1.0.0+1`, 次のパッチリリース `1.0.1+2`

## 3. App Store 審査準備

### 審査用デモアカウント
Appleの審査員がアプリを検証するためのデモ用アカウントが必要です。
1. **アカウント作成**: 検証専用のメールアドレス（`review@example.com` など）でアプリに新規登録します。
2. **テストデータ投入**: 審査員が機能を理解できるよう、サブスクリプションデータ（数件）、タグ、支払い方法を適切に登録してください。
3. **メモの保存**: ログインIDとパスワードを App Store Connect の「審査メモ (Review Notes)」欄に記入してください。

### スクリーンショット撮影リスト
審査およびストア表示用のスクリーンショットを以下の順序で撮影します。
1. **ログイン画面**: 初期画面。
2. **サブスク一覧**: メイン機能（カードレイアウト）。
3. **サブスク登録**: 金額入力などフォーム画面。
4. **支払い方法管理**: 期限警告がある状態。
5. **統計・分析**: タグ別内訳グラフ。
6. **プロフィール**: プライバシーポリシー等へのリンク。

## 4. ビルド手順 (iOS)

1. `flutter clean`
2. `flutter pub get`
3. Xcode で `Runner.xcworkspace` を開く
4. `Product` -> `Archive`
5. App Store Connect にアップロード
